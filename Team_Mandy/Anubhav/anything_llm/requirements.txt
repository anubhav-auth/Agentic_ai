# Core. Or just: pip install -e .
markitdown[pdf,docx,pptx,xlsx]>=0.1.0
PyMuPDF>=1.24.0
openai>=1.0.0
python-dotenv>=1.0.0
watchdog>=4.0.0
httpx>=0.27.0

# Optional: the lab's parser (heavy, pulls ML models).
#   pip install -e ".[docling]"
# docling>=2.0.0
